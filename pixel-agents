#!/usr/bin/env python3
"""
Pixel Agents CLI launcher.

This wrapper should behave like the installed `pixel-agents` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from pixel_cli.main import main
    main()
