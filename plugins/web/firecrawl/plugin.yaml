name: web-firecrawl
version: 1.0.0
description: "Firecrawl web search + content extraction. Supports direct API and Pixel-hosted tool-gateway routing for subscribers. Requires FIRECRAWL_API_KEY (or FIRECRAWL_API_URL for self-hosted), or an active Pixel subscription with FIRECRAWL_GATEWAY_URL."
author: PixelResearch
kind: backend
provides_web_providers:
  - firecrawl
